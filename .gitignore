# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Google OAuth credentials — never commit
credentials.json
gmail_token.json
*.apps.googleusercontent.com.json
mygdrive-*.json

# Microsoft OAuth credentials — never commit
outlook_credentials.json
azure.txt

# Jira / Atlassian credentials — never commit
jira_credentials.json
atlassian.txt
atlassian.ps1
